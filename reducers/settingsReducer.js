import { STATIONS } from '../consts';

export default function settingsReducer(state, action) {
  switch (action.type) {
    case 'SELECT_STATION':
      return state;
    default:
      return state;
  }
}
